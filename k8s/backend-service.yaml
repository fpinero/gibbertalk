apiVersion: v1
kind: Service
metadata:
  name: gibbersound-backend
  labels:
    app: gibbersound
    tier: backend
spec:
  selector:
    app: gibbersound
    tier: backend
  ports:
  - port: 5000
    targetPort: 5000
  type: ClusterIP 